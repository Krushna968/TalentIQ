import { Request, Response, NextFunction } from 'express';

export const analyzePresentation = async (req: Request, res: Response, next: NextFunction) => {};
export const getPresentationHistory = async (req: Request, res: Response, next: NextFunction) => {};
