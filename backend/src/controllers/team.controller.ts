import { Request, Response, NextFunction } from 'express';

export const getTeamContributions = async (req: Request, res: Response, next: NextFunction) => {};
export const getImpactScore = async (req: Request, res: Response, next: NextFunction) => {};
