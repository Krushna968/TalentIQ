export const generateId = (): string => '';
export const slugify = (text: string): string => '';
export const calculatePercentage = (value: number, total: number): number => 0;
export const sanitizeHtml = (input: string): string => '';
